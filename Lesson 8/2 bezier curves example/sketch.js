let step = 200
let w = 800
let h = 800

function setup() {
    createCanvas(w, h)
    background("black")
}

function draw() {
    background("black")
    stroke("white")
    noFill()

    for (let y = step/2; y < w; y += step) {
        for (let x = step/2; x < h; x += step) {
           
        }
        
    }

   
}