function setup() {
    let x = 50
    let y = 50
    let w = 100
    let padding = 25

    createCanvas(1000, 1000)
    noFill()

    rect(x, y, w, w)
    rect(x, y, w + 2 * padding, w + 2 * padding)

    //write your code here
}