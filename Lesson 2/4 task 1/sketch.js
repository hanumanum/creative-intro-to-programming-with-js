


function setup() {
    let x = 50
    let y = 50
    let w = 50
    let padding = 100

    createCanvas(1000, 1000)
    fill("black")

    rect(x, y, w, w)
    rect(x + padding, y, w, w)

    //write your code here
}
